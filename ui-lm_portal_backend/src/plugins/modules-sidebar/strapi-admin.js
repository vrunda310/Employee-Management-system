/**
 * Local plugin (admin entry)
 *
 * IMPORTANT:
 * Strapi's generated `.strapi/client/app.js` imports this file as a **default** export:
 *   import myPlugin from "<path>/strapi-admin";
 *
 * Therefore this file MUST have a default export.
 */

import plugin from './admin/src/index.js';

export default plugin;

